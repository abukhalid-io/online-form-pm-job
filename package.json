{
  "name": "pm-control-panel-apps-script",
  "version": "1.0.0",
  "private": true,
  "description": "clasp tooling untuk sinkron Code.gs ke Apps Script (backend PM Control Panel + PM Gas Analyzer)",
  "scripts": {
    "login": "clasp login",
    "clone": "clasp clone",
    "pull": "clasp pull",
    "push": "clasp push",
    "sync": "clasp push && echo '--- push selesai, jalankan npm run deploy untuk menerbitkan ke /exec ---'",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
